= Semantics
:toc:
:toc-title: Contents
:toclevels: 4
:nofooter:
:stem: latexmath

== What is semantics?

Semantics is the study of meaning in language and logic

* we operate on meanings when we intake a bit of language and have to parse it...
* ...and reason on it -> apply logical operations and produce more language to express output

.Symbolic semantics
Represents knowledge by discrete (enumerable) symbols

* UMLS is a set of enumerable symbols
** it's basically an alphabet
* meaning is defined by explicit logical relationships
** each symbol has one or more meanings
** explaining the meaning may require more symbols/language but not necessarily
* explainable, verifiable, precise, human readable

.Connectionist semantics
Represents knowledge as a point in a *continuous* high dimensional vector space

* extremely robust
** smaller error propagation with smaller differences
* handles nuance
** similar but distinct symbols will be increasingly far from each other
* scales with data
* largely uninterpretable

=== Types of Symbolic Semantics

3 main types:

. Lexical semantics
* given a symbol -> get meaning
* get relation between symbols from meaning
* works on *base case*
** semantics of single words

. Compositional semantics
* composing meanings of single words
** i.e. builds complete semantics of a sentence from single constituent parts
* works *recursively*
** knowing the semantics of these words -> build overall semantics

. Contextual semantics
* looks outside the span of text
** "where does this span come from?"
* the origin of a span of text can change its semantics
* lives in the *global environment*
** not part of the sentence/bit of language

== Lexical Semantics

=== WordNet (1985)
*WordNet* is a structured lexicon/dictionary

* even more structured than normal dictionaries
* makes pointer structure of dictionaries real

Groups words by *meaning* and establishes *relationships* between these groups

. Constitutive
** hyponymy: more specific or instance of (group within group)
** antonymy: opposite
*** gradable, simple, converse
*** not necessarily polar opposites
** synonymy: same meaning
** etc.

. Role
* Agent
** indicates doer of the thing
* Patient
* Result
** indicates the outcome of the thing
* Instrument
** indicates object used to do the thing
* Location
* Direction
* Target
* Source
* Co-role

. Derivation
* diminutive
* augmentative

. Participle

. Pertainyim

* is pertinent to

. Domain
* topic
** thing is relevant in this topic
* region
* exemplifies
** this is an example of this concept

. Other
* mostly for human readability


WordNet has 4 classes (basically the open word classes)

* nouns
* verbs
* adjectives
* adverbs

==== Synsets

A *synset/sense* is a unique cluster of words that can be interchangeably used to describe a particular meaning

A word can be in multiple synsets

* words can have multiple meanings

Synsets contain only one of the 4 classes

* some relationships cross the class boundary

Distinct synset of synonyms

== Case Grammars and Frame Semantics

Larger meanings

* bits of language
** not single terms

=== Filmore's case Grammars (1968)

Verbs are usually classified as:

* transitive
* intransitive

Works syntactically rather than semantically

Filmore proposed *verb cases/frames*

* verbs have properties and structures
** keys are fixed with specific types
** verbs can be filled with references to bits of the sentence

Initial idea worked syntactically but not semantically

* grammars defined with this idea were often unusable

Redefined as *semantic frames*

=== Frame Semantics

Frames are descriptions of a type of:

. event
. or a relation
. or an entity+its participants

Each frame contains:

* a doer
** thing eating
* a doee
** thing being eaten

Each frame consists of:

* lexical units that evoke the frame (LUs)
** basically a list of words that summon the frame
* frame elements (FEs)
** describe the *slots/roles* of the frame
** some slots are optional (or *peripheral*)

==== LUs and Frames

The word *lung* matches the LU *lung.n*

We can find out in which frames the LU appears in

* in this case it could be *body parts*

Which FEs constitute *body parts*?

* *owner* is needed (core)
** this lung obviously belongs to someone
* other supplemental info
** *body part* is also needed (core)
* *descriptor* is peripheral
* *subregion* is peripheral
* *orientational location* is peripheral
* *attachment* is peripheral

Which other LUs *can* serve as body parts?

* many
** LUs can appear in other frames

=== Frame Relations

There are various types of frame relations:

* Inheritance
** body part inherits *shaped part*
** body part is inherited by other frames
* Perspective
* Using
* Subframe
* Metaphor
** metaphors evoke frames linked with literal meaning and metaphorical meaning
* etc.

==== The Concept of Role

Roles are mostly disconnected from syntax

* a sentence with the same meaning can be rewritten with different (legal syntaxes)

The role of *agent* only applies to something with intention

Roles can be used to understand the meaning of a piece of language

* syntax isn't really necessary

We can *reason* on frames using this concept

* semantic roles can push reasoning in different directions
** can ignore/accept different parts (e.g. the subject)

=== Resources

The main resource is *FrameNet* (english)

* developed at Berkeley (again)
* 13,000 word senses
* english has ~160,000 words
** < 10% coverage (cringe)
* 1,200 semantic frames
** not that many
** very few in fact

Other languages have been explored

* not very useful

=== Finding Frames in Text

3 step program:

. Find triggers
* Find LUs that evoke specific frames
. Classify evoked frames
* which frames do these triggers classify
. Extract the argument and fill the slots

There is a Python library that does this as a *seq2seq* task using transformers

* using ML/DL to do *symbolic* tasks
** symbolic tasks are explainable by default
** ML tasks are not
** we lose explainability

=== Limitations

The idea of Frame Semantics is fine

* the application is a human thing
** i.e. not very scalable

As languages evolve -> frames need to evolve

* need to keep up
* highly specialized domains are largely unsupported
** i.e. the medical domain is unsupported (booo)

== Reasoning in Data

WordNet assigns semantics to words + establishes relationships

FrameNet assigns semantics to sentences

So we have semantics (in some way)

We have to *reason* about it + context

We can apply reasoning in code/formulations/produced language

* which is fine

What if we built reasoning rules *into the knowledge base?*

=== Cyc Project (1984)

Created by Douglas Lenat with the aim of creating the "first and most complete General Artificial Intelligence"

The hypothesis was that AI fails because it lacks common sense

* it lacks the unspoken rules we use to navigate reality
** lots of knowledge cannot be easily expressed with language

So they wanted to encode these rules and somehow teach them to an AI

* by 1994 they defined *1,000,000* logical formulas and *100,000* terms [!!!]
* by 2017 they defined *24,500,000* logical formulas and *1,500,000* terms [!!!]
** all by hand (????????)
** the system cannot evolve autonomously -> adding a rule can derail the system

So far there's been no useful applications of this system

* they sell stuff to hospitals and USA health insurance (boooo)
* queries are made in CycL (proprietary language)
* have to find which of the 25M rules work
* also need to sign 7817984 NDAs

TLDR: *CYC IS FUNCTIONALLY USELESS*

* but it's an interesting critter

==== Cyc Architecture

Made of 3 parts:

. Knowledge Base
* real world factual knowledge
* core theories
* domain specific theories
* dynamic data
** data extracted dynamically on request

. Inference Engine (1200 reasoning modules)
* logical engines that reason on information
** deduction
*** classical Aristotelic reasoning (premise + rule => consequence) -> Socrates is a man + all men are mortal => Socrates is mortal
** induction
*** find rule given premise and consequence (scientific method according to British empiricism)
** abduction
*** find premise given a rule and consequence (detective reasoning)

. Intelligent Data Selection
* finds appropriate data for task

Cyc aimed to address the *Knowledge Bottleneck* found in classical expert systems

Early expert systems were brittle

* if system doesn't have rule for thing -> can't infer

Cyc wants to provide the foundational layer

* i.e. common sense

==== CycL language

Formal language based on first order predicate calculus

Uses Lisp-like prefix notation to describe relationships between constants

* like `(#$predicate #$name #$category)`
* also handles quantifiers

Made up of:

. Literals
. Constants
* individuals
* collections
* predicates
* functions
** individuals, collections, and predicates are *individuals* -> can combine them (collections of collections, predicates over functions, etc.)
. Variables
. Sentences

Cyc implements *default logic*

* not boolean
** things can have many values (true, default true, false, default false, undetermined)

Cyc also distinguishes between:

* *default true* -> generally true but can have exceptions
* *monotonically true* -> always true

And has relative exceptions:

* *systematic*
** all <things> do <thing> except <individuals>

==== Contradiction and Micro-Theories

In traditional logic we assume x and not-x cannot be true at the same time

In real world logic we can believe contradictory statements *depending on the context*

Cyc structures knowledge into

* foundational theory (using default logic)
* and lots (~20,000) of micro theories related to domains/concepts
** internally consistent
** can contradict each other

=== Reflection on General Knowledge

Cyc alone is a failure

* general knowledge is too big

Combine Cyc-like symbolic approaches with neural systems

* Hybrid approaches: check LLM answers with symbolic tools
* Neurosymbolic approaches: build ML model that manipulates symbols directly

== Formal Ontologies

Cyc failed but we still need semantic symbolic approaches

* deterministic
* provable
* interoperable

Neural systems don't provide these things

=== Symbolic Imperative

Cyc was too ambitious and complicated

* do simple things well

Keep these from Cyc:

* keep entities and relationships
** with unique identifiers
* reification
** relationships between things are things themselves
* human readable
** nice to be able to understand reasoning simply by reading

We could use a directed graph

* G = <V, E>
** V = entities
** E = edges/relations
** E \subset V = reification
*** edges are also vertices -> relations are things (!)

Add human/machine readable notations and we can get going

=== RDF: Resource Description Framework

Came about ~2000/2010 during semantic web period

Proposed by W3C

* progression of WWW
** linking concepts instead of pages

RDF uses *triples* to define pieces of knowledge

* `(source node, edge type, dest node)`
* which becomes `(subj, pred, obj)`
* can express most structures as triples
** (France, has-capital, Paris)
** (France, has-attribute, capital)
** (capital, has-type, city)
** etc.

Mapped to directional graphs with *URI* or *literals* as values (or blank)

==== RDF Vocab

RDF provides the foundation (syntax)

RDFS provides a schema (semantics)

Initially followed XML notation

* new notations have come out
** Turtle
** SPARQL
